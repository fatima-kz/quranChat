# Client (Expo) — prefixed EXPO_PUBLIC_ are bundled into the app
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_API_URL=http://localhost:3000

# Server only (Vercel) — never exposed to the client
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
